# Navigation ordering hints for the downstream site. This repo is builder-agnostic;
# the SSG decides how to render, but this expresses the intended reading order.
label: Delta Lake
order:
  - explanation      # start with concepts
  - tutorials        # then learn by doing
  - how-to           # then task recipes
  - reference        # then look things up
sections:
  explanation:
    label: Explanation
    order: [what-is-delta-lake, delta-kernel-architecture]
  tutorials:
    label: Tutorials
    order: [your-first-delta-table]
  how-to:
    label: How-to guides
    order: [read-a-delta-table, query-a-table-as-of-version]
  reference:
    label: Reference
    order: [table-features]
