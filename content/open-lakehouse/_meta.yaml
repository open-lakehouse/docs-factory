# Navigation ordering hints for the downstream site. The `open-lakehouse` scope
# holds estate-wide, engine-neutral explanations of the reference model — the
# concepts that aren't specific to a single upstream project.
label: Open Lakehouse
order:
  - explanation      # this scope is explanation-only for now
sections:
  explanation:
    label: Explanation
    order: [catalog, unity-catalog]
