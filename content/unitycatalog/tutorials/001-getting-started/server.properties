server.env=dev
# Authorization disabled for local exploration
server.authorization=disable

# Enable MANAGED tables and set the managed-table storage root
server.managed-table.enabled=true
storage-root.tables=file:///tmp/uc-test
