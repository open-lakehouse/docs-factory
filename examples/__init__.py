# examples — runnable code snippets for open lakehouse content
#
# Each sub-package corresponds to one journey slug (e.g. `delta-lake-quickstart`).
# Within a journey sub-package, each module corresponds to one execution engine
# (e.g. `spark.py`, `daft.py`, `duckdb.py`).
#
# See README.md for the full conventions.
