[project]
name = "examples"
version = "0.1.0"
description = "Runnable code examples for open lakehouse content journeys"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
