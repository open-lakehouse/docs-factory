"""Shared helpers for docs-factory examples.

The example *snippets* live in per-engine directories (``examples/python`` etc.)
and are the copy/paste-runnable source the docs include via region markers. This
package holds only shared test/util code so those directories stay snippet-pure.
"""

from __future__ import annotations

__all__: list[str] = []
