node_modules/
dist/
.vite/
.vercel/

# The build emits routing into .vercel/output/config.json (Build Output API,
# generated by scripts/gen-vercel-config.mjs) — under .vercel/, already ignored
# above. A stray top-level vercel.json would be read BEFORE the build and shadow
# that config, so keep it out of the repo too.
vercel.json

# Prefer a single lockfile if you want reproducibility; keep the harness light.
package-lock.json
bun.lockb
