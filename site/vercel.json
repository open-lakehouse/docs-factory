{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://REPLACE_WITH_NEON_FUNCTION_HOST/:path*" },
    { "source": "/auth/:path*", "destination": "https://REPLACE_WITH_NEON_AUTH_BASE/:path*" },
    { "source": "/((?!assets/|api/|auth/).*)", "destination": "/index.html" }
  ]
}
